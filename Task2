// Задание 2: Создать массив из 3 шаров (красный, синий, зеленый) и поменять красный на желтый

#include <stdio.h>
#include <string.h>

int main() {
    // Создаем массив из 3 строк
    char colors[3][10] = {"red", "blue", "green"};

    // Меняем первый элемент (красный) на желтый
    strcpy(colors[0], "yellow");

    // Выводим массив на экран
    for (int i = 0; i < 3; i++) {
        printf("%s\n", colors[i]);
    }

    /* Вывод
yellow
blue
green
*/

    return 0;
}
