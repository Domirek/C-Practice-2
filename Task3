// Задание 3: Создать массив из 3 чисел (1, 2, 3)

#include <stdio.h>

int main() {
    // Создаем массив из 3 чисел
    int numbers[3] = {1, 2, 3};

    // Выводим массив на экран
    for (int i = 0; i < 3; i++) {
        printf("%d\n", numbers[i]);
    }

   /* Вывод:
1
2
3
*/
    return 0;
}
